apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'

android {
    namespace 'com.hvac.control'
    compileSdk 34 // Must be at least 31 for dependencies, but we can still target 29

    defaultConfig {
        applicationId "com.hvac.control"
        minSdk 23
        targetSdk 29 // Android 10
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    kotlinOptions {
        jvmTarget = '1.8'
    }
}

dependencies {
    implementation 'androidx.core:core-ktx:1.7.0'
    implementation 'androidx.appcompat:appcompat:1.4.1'
    implementation 'com.google.android.material:material:1.5.0'

    // Android Car App Library
    implementation 'androidx.car.app:app:1.1.0'

    // USB Serial library
    implementation 'com.github.mik3y:usb-serial-for-android:3.7.0'
}
